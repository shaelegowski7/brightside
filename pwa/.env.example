# The deployed FastAPI backend's base URL. The shared secret is NOT set here
# deliberately -- it's entered once in the browser and kept in localStorage,
# not baked into the build (see src/api.ts), so it never ends up inlined
# into the publicly-fetchable JS bundle.
VITE_API_BASE_URL=https://your-backend.up.railway.app
